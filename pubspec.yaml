name: mobilitem2miage
description: Projet de mobilité - M2 MIAGE.
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # State Management
  provider: ^4.3.2+2

  # Firebase
  firebase_core : ^0.5.1
  firebase_analytics : ^6.1.0
  firebase_auth : ^0.18.2
  cloud_firestore : ^0.14.2

  # Auth
  google_sign_in: ^4.5.6

  # Map & Location
  google_maps_flutter: ^0.5.33
  location: ^3.0.0
  flutter_google_places: ^0.2.6

  # Hash
  crypto: ^2.1.0
  common_utils: ^1.2.1

  # Dependency Injection
  get_it: ^1.0.3

  # Icons & Font
  font_awesome_flutter: ^8.10.0
  cupertino_icons: ^1.0.0
  google_fonts: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true


  assets:
    - res/assets/maps/retro.json

    - res/assets/maps/markers/gym_b.png
    - res/assets/maps/markers/touristattraction_b.png
    - res/assets/maps/markers/restaurant_b.png
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware.

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
